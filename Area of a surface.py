#   Enter the width and length of a surface. The program should compute the area. 
# Enter the values as floating point numbers and print the units of mesurement used.

width = float(input('Enter the Width: '))
length = float(input('Enter the length: '))
print('Area of the surface: ' + str( width * length))